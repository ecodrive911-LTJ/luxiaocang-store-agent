# 融合部署记录：D2-01 巡检 + D2-3 视频闭环 + F1 审计 + 鲁棒 F2

日期：2026-07-15
操作人：店参谋 Agent
仓库：luxiaocang-store-agent（local master → origin/main）

## 背景（阻塞点）
- 本地 master `0ae7f44` 已部署 D2-01（巡检引擎）+ 鲁棒 F2（`_upsert_price_data` 带 INSERT），但 Git push 被拒：`master` 落后 `origin/main` 两个提交（远程 `4135a94` v0.7 D2-3 + `5d321fe`）。
- 远程两个新提交包含：D2-3 AI 视频分析闭环（新增 `scripts/analyze_video.py` + `_write_price_data` INSERT）、F1 审计（create_task 调 log_audit）、以及远程版 F2。
- 关键点：远程 F2 是 `UPDATE price_data ... WHERE upload_id=?`（UPDATE-only）：当 AI 分析失败（price_data 无该 upload 记录）时 UPDATE 找不到行、无效 → **远程 F2 实际未真正闭环**，与原先发现的 F2 bug 本质相同。

## 冲突分析
`git merge origin/main` 仅 `app.py` 冲突（3 处，全部在 `confirm_upload_review`）：
1. 仅确认分支：本地 `_upsert_price_data`（INSERT+UPDATE） vs 远程 `UPDATE price_data ... ai_confidence=1.0`
2. 注释行：保留远程说明
3. 人工修正分支：本地 `_upsert_price_data` vs 远程 `UPDATE price_data ... COALESCE`

决策：**三处冲突均采纳本地 `_upsert_price_data`**，因其在 AI 失败时仍能写入 price_data，真正闭环；远程 UPDATE-only 在失败时无效。

## 融合结果（app.py，merge commit 236871c）
| 来源 | 组件 | 状态 |
|---|---|---|
| 本地 D2-01 | message_queue 表+索引 / run_inspection / scheduler_loop / POST /api/inspection/run / GET /api/messages / 启动调度 | ✅ 保留 |
| 本地 F2 | `_upsert_price_data`（INSERT ON CONFLICT） | ✅ 保留（优于远程） |
| 远程 D2-3 | analyze_video.py 调用 + `_write_price_data` 名优店比价 INSERT | ✅ 引入 |
| 远程 F1 | create_task 审计 log_audit | ✅ 引入 |
| 冲突 | app.py 3 处已解（confirm_upload_review 两处 + 注释） | ✅ 已解 |

辅助处理：
- `static/index.html` 本地有未提交改动（修复 Store_owner 门店切换器禁用状态残留）→ 先单独 commit 后 merge（自动合并成功）。
- `scripts/analyze_video.py` 本地有同名未跟踪文件 → 备份为 `_orig_analyze_video.py`，merge 带入远程权威版。

## Git
- merge commit `236871c`，`git push origin master:main` 成功：`5d321fe..236871c`，本地/远程 SHA 一致（MATCH_OK）。

## 部署（阿里云 ECS 120.26.176.215）
- SFTP 上传：`app.py`、`scripts/analyze_video.py`（D2-3 核心，服务器此前缺失）、`static/index.html`
- 重启 `luxiaocang.service`，新 PID `129346`，Active: active (running)，启动完整无异常。

## 端到端验证（直连 API）
- 登录 200 ✅
- 巡检 `POST /api/inspection/run` → `{scanned:2, alerts:1}` ✅（D2-01 在线）
- 消息 `GET /api/messages` → count=1（告警入 message_queue）✅
- F2 闭环：某 collect_uploads 的 price_data 在 confirm 前 = 0（AI 失败场景），confirm 人工修正后 price_data 出现 `('MC_F2', 2.5, 1.0)` ✅
- 测试数据已清理（price_data + message_queue）

## 结论
融合版已上线且功能完整：D2-01 巡检引擎、鲁棒 F2 闭环、D2-3 视频分析代码、F1 审计全部就位。GitHub 与阿里云均已同步。

## 遗留待修
- **F3**：服务器仍缺 `ffmpeg`。D2-3 的 `analyze_video.py` 依赖 ffmpeg 抽帧，运行时视频 AI 分析仍会失败；当前由 F2（confirm 人工兜底）保证 price_data 不空。装 ffmpeg 后 D2-3 端到端方能真正跑通。
- 远程 F2（UPDATE-only）已被本地 `_upsert` 覆盖，无需另行修复。

## 下一步候选
1. 安装 ffmpeg（F3）→ 让 D2-3 AI 视频分析端到端跑通
2. 推进 D2-02（消息中心 UI） / D2-03 其余子项
3. 标记路线图进度（D2-01 已完成并上线）
