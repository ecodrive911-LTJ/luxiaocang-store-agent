# 阶段1 Day1+D3-1 执行记录 (2026-07-15)

## 目标
按 v2.1 路线图补齐 6 项短板，先攻阶段1（安全/数据/质量/闭环）。

## 已完成 ✅

### D1 后端安全与权限隔离 (全部完成)
- **D1-1 前端 RBAC 动态渲染**：store_owner 仅见📋任务，模块1/2/6隐藏 → 用户截图验证通过
- **D1-2 后端 API 角色隔离**：`/api/data/assets` 仅返回绑定门店；store_owner 越权访问被强制覆写为广安店（verify_chat_sse.py 5项全过）
- **D1-3 Path bug 修复**：`agent_loop.py` 补 `from pathlib import Path`，重启服务生效（PID 122731）
- **D1-4 /api/chat store_owner 强制绑定门店**：不传/传错 store_id 均强制覆写为唯一绑定门店
- **D1-5 操作审计日志**：新增 `audit_logs` 表 + `/api/audit/logs`(admin)、`/api/audit/stats`(admin)、login/chat 自动记录；owner 访问被 403 拒绝（verify_audit.py 全过）

### D3-1 视频分析全链路 + 人工复核 (后端+前端完成)
- 后端：`/api/uploads/pending`(manager)、`/api/uploads/{id}/review`(manager)、`/api/uploads/{id}/confirm`(manager, 支持AI结果接受/人工修正)、`/api/uploads/{id}/reprocess`(manager)
- 前端：任务详情上传记录加「复核」按钮(仅manager/admin)；复核弹窗显示AI解析JSON + 人工修正表单 + 确认/重分析
- 修复2个SQL bug：`collect_tasks` 用 `title` 非 `task_name`；`collect_task_items` 无 `expected_price` 列
- smoke_test 全过（login/me/audit/logs/uploads/pending/tasks 均 200）

### D2-4 失败重试机制
- `analyze_upload_video` 增加重试：最多3次尝试，失败转 `retrying` 状态 + 5秒退避，耗尽转 `failed`
- 前端状态图增加 `retrying`(🔁重试中)

### D2-5 部署联调
- SFTP 部署成功（app.py/agent_loop.py/static/index.html），服务运行中 PID 122731

## 验证脚本（本地 diancanmou/）
- verify_chat_sse.py → D1-2/1-4 越权覆写 5项全过
- verify_audit.py → D1-5 审计 5项全过
- verify_review.py → D3-1 权限/404 全过
- smoke_test.py → 系统冒烟全过

## Git 状态
- commit `51f2702` "v0.5.2 D1-5审计日志+复核界面+D2-4失败重试" 已本地提交
- **push 失败**：GitHub token 在 URL 中但 git 回退交互式密码提示（/dev/tty 不可用），反复出现的网络/凭证问题，待鹿总确认是否重试

## 待办（阶段1 剩余）
- **D3-2 比价矩阵 + 三档定价**：`price_data` 表已有，需前端比价矩阵视图 + 低/中/高三档建议定价算法
- **D3-3 全链路测试**：真实视频上传→AI解析→人工复核→入库 端到端（需真实录屏样本）
- **D3-4 自动化回归测试**：pytest 用例覆盖 RBAC 越权/审计/复核
- **D2 剩余**：失败反馈前端 toast 细化、超时配置
