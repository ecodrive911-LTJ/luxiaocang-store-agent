
---

## 2026-07-15 16:03 D3-2 比价矩阵前端验证

### 完成
- [x] D3-2 比价矩阵 后端+前端 部署到阿里云
- [x] admin 登录态下浏览器自主验证通过
  - 顶部「?? 比价矩阵」按钮显示
  - 弹窗「?? 比价矩阵 · 三档定价建议」正常弹出
  - 颜色图例：绿色=引流档 蓝色=利润档 橙色=形象档
  - price_data 为空时正确显示「暂无比价数据。请先通过任务采集竞品价格。」
  - 关闭、?? 刷新 按钮可见可点
- 截图证据已留
- GitHub commit 34f54f0 推送成功

### 顺带挖出的 D1-1 遗留 bug（待修）
1. **storeSelector admin 未启用**：L1057-1062 只对 store_owner 设 disabled=true，没有 else 分支给 admin/manager 显式 disabled=false。store_owner 登出后 admin 登录时，DOM 残留 disabled 状态，admin 也无法切换门店。
2. **title 乱码**：L1061 sel.title = '鍒嗗簵搴椾富涓嶅彲鍒囨崲闂ㄥ簵' 真实文字「分店店主不可切换门店」，UTF-8 编码异常。

### 影响
- admin 用户无法在 UI 切换门店（实际 API 端能调，只是 UI disabled）
- store_owner 鼠标悬停选择器时显示乱码 tooltip

### 建议修复方案（1 行代码）
在 L1062 后追加：
`javascript
} else {
  sel.disabled = false;
  sel.style.opacity = '1';
  sel.style.cursor = 'pointer';
  sel.title = '切换门店';
}
`
乱码问题：用 ASCII 或统一 UTF-8 重新写。

### 下一步
- 等鹿总决策：是否修复 D1-1 这两个 bug
- D3-3 全链路测试 / D3-4 自动化回归测试待开工
