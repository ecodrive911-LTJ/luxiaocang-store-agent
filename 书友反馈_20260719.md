# 书友反馈整理（2026-07-19）

> 来源：2026-07-19 对话，针对"产品定位 / 自身定位 / 盈利引擎 / 日志 / 推送"五大问题的回复
> 整理人：WorkBuddy Agent
> 状态：**待书友确认**，确认后按此执行

---

## 一、对 Agent 上轮汇报的逐条反馈

### 1. 产品定位 ✅ 正确
- **鹿小仓·店参谋 = 便利店全生命周期 AI 经营参谋**
- 从选址开新店 → 商品选品 → 定价 → 美团平台配合 → 巡检告警 → 财务算账，全流程覆盖
- 工作方向没有问题，继续按此推进

### 2. 自身定位（WorkBuddy vs 鹿小仓）✅ 正确
- **WorkBuddy = 开发工具 / 工作台**（你我在里面干活的地方）
- **鹿小仓 = 独立部署在阿里云 ECS 上的产品**（120.26.176.215:/opt/luxiaocang/）
- WorkBuddy **不是**鹿小仓本身，而是帮鹿小仓写代码、改代码、部署的工具
- 这个边界清晰，不混淆

### 3. 盈利引擎的定位 —— 已澄清
- **盈利引擎 = 我（WorkBuddy）刚给你集成的那个 Skill 包**
  - 来源：你交过来的 `dynamic_profit_engine_skill/` 四件套（README / tool_schemas / core_logic / config_example）
  - 落盘：`profit_tools.py` + `dynamic_profit_engine.py`（4 个工具：evaluate_order / get_store_dashboard / simulate_price_change / simulate_delivery_strategy）
  - 已写入鹿小仓的 `agent_loop.py`（system prompt 扩展）和 `app.py`（建表 + 摄入端点 + 门店注入）
- **定位**：鹿小仓对话链路中的一个能力模块（"总会计师/总风控官/总军师"三合一财务管家）
- **独立部署**：目前暂不考虑，未来有需要再说

### 4. Skill 扩展方向 —— 重要认知
- **未来会持续增加其他 Skills**，增强 Agent 的线上运营辅助能力
- 盈利引擎只是第一个核心 Skill，不是最后一个
- **隐含要求**：Skill 集成架构要有扩展性，后续接新 Skill 要方便
- **当前已实现的扩展机制**：
  - `@register_tool` 装饰器注册工具到全局 `TOOLS = {}`
  - 自写协议 + OpenAI schema 双轨留底（未来迁移只改调用层）
  - 外部数据被动摄入端点 `/api/profit_engine/ingest`

### 5. 日志更新 —— 刚性要求（强约束）
**必须做到**：
- 每一次核心文件改动 → 立刻写日志
- 每一次 .md 文件改动 → 立刻写日志
- 每一次数据结构调整 → 立刻写日志
- **双写位置**：`TASKBOARD.md` 变更记录 + `memory/YYYY-MM-DD.md` 当日日志
- **启动刚性条件**：每次新会话启动，Agent 必须先读日志（TASKBOARD + 当日 memory），作为继续工作的上下文依据
- **建议写入**：`WORKFLOW_MANDATORY.md` 或 `工程师交接总纲_README.md` 作为启动协议

### 6. 推送 / 部署问题 —— 协作机制
- 推送 GitHub / SFTP 上传 / systemctl restart 过程中，**如果线上没反应或出现异常**
- **立刻向书友反馈**，不要闷头排查，书友会协助解决
- 不要假装没问题、也不要拖延

---

## 二、当前工作状态标注

### 🟡 当前状态：暂停 → 待书友确认后重启

| 事项 | 状态 | 说明 |
|------|------|------|
| 盈利引擎代码 | ✅ 本地写完 | `profit_tools.py` / `dynamic_profit_engine.py` / `agent_loop.py` 扩展 / `app.py` 建表+端点 |
| 本地测试 | 🟡 3/4 通过 | 第 4 个工具 `simulate_delivery_strategy` 的键名 bug 已修，待重跑 |
| 5 步链路-步骤 1 | 🟡 待闭环 | 重跑测试通过即可闭环 |
| 5 步链路-步骤 2 | ❌ 未开始 | git push origin master:main |
| 5 步链路-步骤 3 | ❌ 未开始 | SFTP 上传 + systemctl restart |
| 5 步链路-步骤 4 | ❌ 未开始 | 线上场景验证 |
| 5 步链路-步骤 5 | ❌ 未开始 | TASKBOARD + memory 双写变更日志 |

### 🔴 明确标注的问题
1. **测试未闭环**：第 4 个工具的 bug 已修但没重跑验证
2. **日志未更新**：盈利引擎集成的所有改动都还没写进 TASKBOARD 和 memory
3. **启动协议未固化**：书友刚提出的"启动必读日志"要求还没写进 WORKFLOW_MANDATORY.md

### 🟢 无障碍、可继续推进的事项
- 重跑测试 → 步骤 1 闭环
- GitHub push（如遇问题立刻反馈给书友）
- SFTP 上传 + restart
- 线上验证
- 双写变更日志
- 把"启动必读日志"写入 WORKFLOW_MANDATORY.md

---

## 三、下一步执行计划（待确认后执行）

### Phase A：先把"启动协议"固化（5 分钟）
- 修改 `WORKFLOW_MANDATORY.md` 新增"六、Agent 启动协议"章节
- 内容：每次新会话启动，Agent 必须先读 TASKBOARD.md + 当日 memory/YYYY-MM-DD.md
- 作为刚性启动条件，与 5 步链路同级优先

### Phase B：重启盈利引擎 5 步链路（核心任务）
- **步骤 1 闭环**：重跑 `test_profit_engine.py`，确认 4 个工具全部通过
- **步骤 2**：`git add -A` → `git commit` → `git push origin master:main`（遇阻立刻反馈）
- **步骤 3**：`python upload_to_server.py`（补上 `dynamic_profit_engine.py` / `profit_tools.py`）+ `systemctl restart luxiaocang.service`（通过 SSH）
- **步骤 4**：线上场景验证
  - 发"5 公里单子能不能接" → 验证 evaluate_order 调用
  - POST `/api/profit_engine/ingest` 模拟数据摄入
  - 查 dashboard 返回 3 天累计数据
- **步骤 5**：双写变更日志
  - `TASKBOARD.md` 变更记录追加盈利引擎集成条目
  - `memory/2026-07-19.md` 追加同日记录

### Phase C：盈利引擎 Skill 集成架构说明（附在 TASKBOARD）
- 记录 Skill 扩展机制：`@register_tool` 装饰器 + 双轨协议
- 说明后续接新 Skill 的标准流程
- 为未来更多 Skills 留好文档基础

---

## 四、请书友确认

请逐条核对：

- [ ] 1. 产品定位理解无误
- [ ] 2. WorkBuddy vs 鹿小仓的定位边界无误
- [ ] 3. 盈利引擎 = 刚集成的 Skill 包，目前不独立部署
- [ ] 4. 未来会有更多 Skills，架构要有扩展性
- [ ] 5. 日志双写 + 启动必读日志 作为刚性要求
- [ ] 6. 推送遇阻立刻反馈，不闷头排查
- [ ] 7. 同意按 Phase A → B → C 顺序重启工作

**确认后我立刻执行，不再等待。**

---

**整理时间**：2026-07-19 11:51
**整理人**：WorkBuddy Agent
**状态**：⏸️ 暂停，待书友确认
